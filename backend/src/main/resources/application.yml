spring:
  application:
    name: unified-portal-backend
  
  # MongoDB配置
  data:
    mongodb:
      host: zha-vm36-w11v
      port: 27017
      database: unified_demo_1
      # 如果需要认证，取消注释以下配置
      username: unifiedWriter
      password: xQ5GcH1xP7gkJdPb3yph5jCl
      # authentication-database: admin
  
  # 静态资源配置
  web:
    resources:
      static-locations: classpath:/static/
  
  # Jackson配置
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss

# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

# JWT配置
jwt:
  secret: your-secret-key-change-this-in-production-to-a-very-long-random-string
  expiration: 86400000 # 24小时（毫秒）

# 日志配置
logging:
  level:
    root: INFO
    com.example.backend: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'
